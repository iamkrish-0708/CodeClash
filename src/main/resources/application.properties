spring.application.name=codeclash

# Server Port
server.port=${PORT:8080}
server.address=0.0.0.0

# Default Database: Embedded Zero-Config H2 (compatible with MySQL mode) or Cloud DB
spring.datasource.url=${DATABASE_URL:jdbc:h2:file:./data/codeclash;DB_CLOSE_DELAY=-1;MODE=MySQL}
spring.datasource.driver-class-name=${DATABASE_DRIVER:org.h2.Driver}
spring.datasource.username=${DATABASE_USERNAME:sa}
spring.datasource.password=${DATABASE_PASSWORD:}

# Hikari Connection Pool Settings & Timeouts
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=true

# Upload Limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Google Sign-In OAuth Client ID
google.client.id=${GOOGLE_CLIENT_ID:258922497438-asurq360ieu36s8i39b78c13avpmab5c.apps.googleusercontent.com}
